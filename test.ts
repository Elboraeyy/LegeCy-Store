import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function test() { try { const p = await prisma.product.findFirst(); await prisma.stockNotification.create({ data: { whatsapp: '01279342199', channel: 'whatsapp', productId: p.id, status: 'pending' } }); console.log('Success'); } catch(e) { console.error('Error:', e); } finally { await prisma.\$disconnect(); } } test();
